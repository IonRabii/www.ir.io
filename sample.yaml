# sample.yaml
get:
  summary: Retrieve a sample
  operationId: getSample
  responses:
    '200':
      description: A successful response
      content:
        application/json:
          schema:
            type: object
            properties:
              message:
                type: string
                example: 'Hello, world!'
