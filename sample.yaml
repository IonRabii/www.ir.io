# sample.yaml
post:
  summary: Retrieve a remote sample
  operationId: getSample-remote
  responses:
    '200':
      description: A successful response
      content:
        application/json:
          schema:
            type: object
            properties:
              message:
                type: string
                example: 'Hello, world!'
